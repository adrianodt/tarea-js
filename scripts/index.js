
/* let arrayEventos = data.events;

crearCard(arrayEventos, "#container-cards");

let checkbuttons = document.querySelectorAll("input[type=checkbox]")

console.log(checkbuttons)

checkbuttons.forEach(check => check.addEventListener("change", verificarSeleccion))


function verificarSeleccion(){
    let seleccionado = Array.from(checkbuttons).filter(check => check.checked)

    let arrayEventosFiltrados = filtrarEventos(arrayEventos,seleccionado[0].value)

    console.log(arrayEventosFiltrados);

    crearCard(arrayEventosFiltrados,"#containercards")
    
}

function filtrarEventos(arrayEventos, valor) {
    let arrayEventosFiltrados = arrayEventos.filter(evento => evento.category == valor);
      
    return arrayEventosFiltrados;
    
} */

